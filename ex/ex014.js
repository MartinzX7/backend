function areaTrianguloAnonima(base, altura) {
    let area = (base * altura) / 2;
    return area;
}

console.log(areaTrianguloAnonima(10, 5));

const areaTriangulo = (base, altura) => {
    let area = (base * altura) / 2;
    return area;
}

console.log(areaTriangulo(10, 5));